export declare const registerAuthIpc: () => void;
/** Stop the repeat at shutdown. The timer is unref'ed, so this is tidiness
 *  rather than a requirement for the process to exit. */
export declare const disposeAuth: () => void;
